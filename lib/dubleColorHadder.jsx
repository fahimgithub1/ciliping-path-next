

export default function DubleColorHadder(props){

    return(
        <h1 className="DubleColorHadder SectionTitle">
            {props.hedderText} 
            <span>{props.spanText}</span>
        </h1>
    )
}